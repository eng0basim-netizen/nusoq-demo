(()=>{
'use strict';
function progress(on,text){let x=document.querySelector('#npdf-progress');if(!x){x=document.createElement('div');x.id='npdf-progress';x.style.cssText='position:fixed;inset:0;background:#101828bb;z-index:999999;display:none;place-items:center;font-family:Arial';x.innerHTML='<div style="background:white;padding:22px 28px;border-radius:14px;min-width:300px;text-align:center"><b id="npdf-progress-text">Preparing PDF…</b><div style="font-size:11px;color:#667085;margin-top:7px">Using the same A4 pages shown in preview</div></div>';document.body.appendChild(x)}x.style.display=on?'grid':'none';document.querySelector('#npdf-progress-text').textContent=text||'Preparing PDF…'}
async function waitImages(root){const imgs=[...root.querySelectorAll('img')];await Promise.all(imgs.map(img=>img.complete?Promise.resolve():new Promise(r=>{img.onload=img.onerror=r})))}
async function generate(){if(!window.NUSOQ_DOC_ENGINE)return alert('Page engine is still loading.');if(!window.html2canvas||!window.jspdf?.jsPDF)return alert('PDF engine is still loading.');let stage;try{progress(true,'Building the same A4 pages as preview…');if(document.fonts?.ready)await document.fonts.ready;const out=window.NUSOQ_DOC_ENGINE.buildStage();stage=out.stage;await waitImages(stage);await new Promise(r=>requestAnimationFrame(()=>requestAnimationFrame(r)));const pages=out.pages;if(!pages.length)return alert('No BOQ pages found.');const land=out.state.ori==='l',pdf=new window.jspdf.jsPDF({orientation:land?'landscape':'portrait',unit:'mm',format:'a4',compress:true});for(let i=0;i<pages.length;i++){progress(true,`Generating PDF ${i+1} / ${pages.length}`);if(i)pdf.addPage('a4',land?'landscape':'portrait');const canvas=await html2canvas(pages[i],{scale:1.5,backgroundColor:'#fff',useCORS:true,logging:false});pdf.addImage(canvas.toDataURL('image/jpeg',.94),'JPEG',0,0,land?297:210,land?210:297,undefined,'FAST');canvas.width=canvas.height=1;await new Promise(r=>setTimeout(r,0))}const s=out.state,name=(s.project?.pno||'Nusoq_BOQ').replace(/[^a-z0-9_-]/gi,'_');pdf.save(name+'_BOQ.pdf')}catch(err){console.error(err);alert('Unable to generate PDF. Please try again.')}finally{stage?.remove();progress(false)}}
function wire(){const b=document.querySelector('#pdfExportBtn');if(!b)return false;b.onclick=e=>{e.preventDefault();generate()};return true}
let tries=0;const timer=setInterval(()=>{tries++;if(wire()||tries>100)clearInterval(timer)},100);
window.NUSOQ_GENERATE_PDF=generate;
})();